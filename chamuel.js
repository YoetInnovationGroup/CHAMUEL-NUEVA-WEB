const menu=document.getElementById('chamuel-menu');const toggles=[...document.querySelectorAll('.menu-toggle')];function closeMenu(){menu.hidden=true;toggles.forEach(b=>b.setAttribute('aria-expanded','false'));}toggles.forEach(b=>b.addEventListener('click',()=>{menu.hidden=!menu.hidden;toggles.forEach(t=>t.setAttribute('aria-expanded',String(!menu.hidden)));}));menu.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));document.addEventListener('keydown',e=>{if(e.key==='Escape'){closeMenu();toggles.find(b=>b.offsetParent)?.focus();}});document.addEventListener('click',e=>{if(!menu.contains(e.target)&&!toggles.some(b=>b.contains(e.target)))closeMenu();});document.getElementById('admission-form').addEventListener('submit',e=>{e.preventDefault();const f=new FormData(e.target);const name=String(f.get('name')||'').trim();const consultation=String(f.get('message')||'').trim();const message=['Hola, me gustaría solicitar una consulta en Chamuel.','','Nombre completo: '+name,'¿Cómo podemos ayudarle?: '+consultation].join('\n');window.open('https://wa.me/50664836199?text='+encodeURIComponent(message),'_blank','noopener,noreferrer');});